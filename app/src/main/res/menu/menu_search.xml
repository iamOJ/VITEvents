<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="im.rohan.chapters.MainActivity">
    <!--<item android:id="@+id/search"
        android:title="Search"
        app:actionViewClass="android.support.v7.widget.SearchView"
        android:icon="@mipmap/ic_launcher"
        android:orderInCategory="100" app:showAsAction="ifRoom|collapseActionView" />
    -->

    <item android:id="@+id/addEvent"
        android:title="Add Event"
        />
    <item android:id="@+id/addClub"
        android:title="Add Club"
        />
    <item android:id="@+id/addTeacher"
        android:title="Add Teacher"
        />
    <item android:id="@+id/addStudent"
        android:title="Add Student"/>
</menu>
